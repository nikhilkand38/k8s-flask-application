apiVersion: v1
kind: Service
metadata:
  name: tanuja-service
spec:
  type: NodePort
  selector:
      app: tanuja
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080

